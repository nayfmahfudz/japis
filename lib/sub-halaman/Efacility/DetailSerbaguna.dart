import 'package:flutter/material.dart';

class DetailSerbaguna extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),
        body: Image.asset('gambar/fasilitas/gedung-serbaguna.jpeg'),
      ),
    );
  }
}
