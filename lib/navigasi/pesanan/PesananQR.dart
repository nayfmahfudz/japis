import 'package:flutter/material.dart';

class PesananQR extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('gambar/qr.jpeg'),
    );
  }
}
